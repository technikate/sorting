describe("Bubble Sort", function () {
  it("handles an empty array", function () {
    expect(bubbleSort([])).toEqual([]);
  });

  // it(
  //   ("sorts the array",
  //   function () {
  //     let testArray = [3, 2, 5, 6, 7, 4, 1];
  //     expect(bubbleSort(testArray)).toEqual([1, 2, 3, 4, 5, 6, 7]);
  //   })
  // );
});
